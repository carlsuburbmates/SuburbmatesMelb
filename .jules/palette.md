## 2024-03-29 - Added Aria Labels to Search
**Learning:** Found an `aria-label` missing on the Directory search input, and its corresponding clear button, with decorative icons read to screen readers.
**Action:** Always verify `aria-label` attributes on inputs without `<label>` tags and icon-only buttons, and use `aria-hidden` on decorative SVG icons to improve screen reader experience.
