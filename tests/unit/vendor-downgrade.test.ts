import { describe, it } from "vitest";

// Integration for enforceTierProductCap lives in Playwright suites once the
// downgrade webhook harness is wired up. Keep a placeholder suite here so
// vitest does not flag the file as empty.
describe.skip("vendor-downgrade", () => {
  it("pending implementation", () => {});
});
