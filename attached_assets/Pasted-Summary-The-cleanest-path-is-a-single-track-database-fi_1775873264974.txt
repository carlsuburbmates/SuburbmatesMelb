Summary

The cleanest path is a single-track, database-first stabilization sequence with hard gates. Do not rebuild UI, do not add new features, and do not run destructive schema cleanup until the repo is first reconciled with the actual remote Supabase truth. The fastest way to stop the back-and-forth is to separate truth layers cleanly: remote Supabase = database truth, GitHub = code/docs truth, Replit = execution workspace only, this chat = audit/spec layer.  ￼  ￼
Your next work should be one controlled pass: remote verify → regenerate types → fix runtime bugs → freeze canonical DB doc → then remove dead schema/code in batches.  ￼  ￼  ￼

Detailed Breakdown

The cleanest path forward

Use this exact order and do not skip ahead.

Phase 1 — Freeze truth before touching structure

Create one canonical repo doc called something like:

docs/DATABASE_TRUTH.md

It should record only:
	•	current active tables
	•	current active columns
	•	current compatibility columns still tolerated
	•	deprecated tables/columns/functions
	•	auth/public.users rule
	•	region/suburb rule
	•	featured eligibility rule
	•	what is product language vs technical legacy naming

Why first:
	•	the SSOT is newer than parts of the codebase
	•	the generated DB types still expose older schema residue
	•	the repo already includes a dedicated remote audit script because local assumptions are not enough  ￼  ￼  ￼

Phase 2 — Re-verify the remote database and regenerate types

Before any structural cleanup:
	1.	run the remote audit again
	2.	regenerate src/lib/database.types.ts from the live remote database
	3.	compare the regenerated types to the audit report

This is the hard gate.
No schema drop, no migration surgery, no “organization” cleanup before this.

Why:
	•	the repo contains both modern cleanup migrations and stale legacy schema assumptions
	•	destructive changes based on stale local types are the fastest way to break everything again  ￼  ￼  ￼

Phase 3 — Fix only the live runtime correctness bugs

Do one small, non-destructive cleanup batch first.

That batch should include only:
	1.	fix redirect route boolean filter
	2.	switch inline admin clients to the shared supabaseAdmin
	3.	restore daily shuffle behavior in the main directory search
	4.	remove any dead tier field leaking into active analytics/search responses if still present

Why these first:
	•	they are active runtime mismatches
	•	they are safer than schema deletion
	•	they directly improve production behavior without forcing irreversible DB changes

Evidence:
	•	redirect route uses .is('is_archived', false) instead of a boolean equality check and creates its own admin client inline  ￼
	•	creator API route also creates its own admin client inline  ￼
	•	directory search orders by created_at DESC instead of daily shuffle logic, despite SSOT requiring daily randomized default discovery  ￼  ￼
	•	shared Supabase client helpers already exist in src/lib/supabase.ts  ￼

Phase 4 — Quarantine auth drift before rewriting onboarding

Do not redesign auth yet. First isolate what is dead.

Specifically:
	•	mark src/lib/auth.ts as legacy or remove it only after confirming active imports
	•	do not let password-based auth remain an active competing path if the intended model is magic link / OAuth
	•	do not start claim/create onboarding build until this is resolved

Why:
	•	SSOT says creator auth should be magic-link/OAuth-first
	•	the repo still contains password-based sign-up and sign-in in src/lib/auth.ts  ￼  ￼

Phase 5 — Only then do destructive schema cleanup

After Phases 1–4 are complete, do one deliberate remote cleanup batch.

This is where you decide, table by table and column by column:
	•	what truly has no active code path
	•	what has no current operational use
	•	what can be dropped safely

Candidates are likely:
	•	legacy vendor columns
	•	business_profiles.vendor_tier
	•	business_profiles.is_vendor
	•	possibly appeals
	•	possibly reviews
	•	possibly legacy product/image compatibility columns

But do not drop these just because they look old in the repo. Drop only after:
	•	fresh remote audit
	•	code reference audit
	•	data presence check
	•	migration plan approved

Why:
	•	the repo shows transitional cleanup, not a fully reconciled end state  ￼  ￼  ￼  ￼

Phase 6 — Regenerate types after every schema change

This is mandatory.
Every remote schema change must be followed by:
	•	regenerate types
	•	update DATABASE_TRUTH.md
	•	verify affected API routes
	•	verify search and redirect behavior

Otherwise you will reintroduce the same drift immediately.

Phase 7 — Only after database stabilization, move to product rebuild

Once the database truth is stable, then move to:
	•	creator claim/create onboarding
	•	category-first discovery
	•	region-secondary discovery
	•	featured eligibility enforcement by suburb + postcode → region
	•	seeded + claimable launch flow

Do not build these on top of a drifting database layer.

⸻

The operating model that avoids back and forth

Use these rules:

One source of truth per layer
	•	Remote Supabase = database truth
	•	GitHub = code/docs truth
	•	Replit = implementation workspace only
	•	ChatGPT = audit/specification/review layer

One branch for one purpose

Use one branch only for this phase, something like:
	•	db-stabilization-pass

Do not mix:
	•	UI rebuild
	•	onboarding redesign
	•	feature work
	•	styling changes

One PR sequence

Create only two PRs in this order:
	1.	PR 1: non-destructive runtime stabilization
	2.	PR 2: remote-aligned schema cleanup

No third stream until both are merged and verified.

⸻

What not to do

Do not:
	•	start rebuilding UI now
	•	add new onboarding flows now
	•	rename every “vendor” concept right now
	•	create more tools/apps/workspaces
	•	trust old migrations as current truth
	•	drop remote tables based only on repo vibes

The old RLS migration alone proves the repo still carries stale assumptions from the marketplace era.  ￼

⸻

The exact next action

Use Replit on the real repo for this one task only:

Implement Phase 1–3 only
	•	write docs/DATABASE_TRUTH.md
	•	re-run remote audit + regenerate types
	•	fix redirect bug
	•	replace inline admin clients with shared supabaseAdmin
	•	restore daily shuffle behavior
	•	produce a post-fix report of remaining destructive cleanup candidates
	•	stop there

That is the cleanest path because it gives you:
	•	stable documentation
	•	stable runtime
	•	a verified base for later cleanup
	•	no schema-risk guessing

Sources

Current SSOT and mandatory remote-authoritative rules:  ￼
Generated DB types showing legacy residue:  ￼
Shared Supabase client helper:  ￼
Legacy password auth path:  ￼
Stale legacy RLS migration:  ￼
Remote audit script:  ￼
Products alignment migration:  ￼
Tier cleanup migration:  ￼
Integrity remediation migration:  ￼
Current search implementation:  ￼
Redirect route bug and inline client:  ￼
Creator route inline client:  ￼

Integrity Footer

Known: the repo is in a mixed transitional state; runtime bugs and architecture drift are directly visible in the live code, and the repo already acknowledges that remote DB truth must be audited explicitly.
Inferred: the least wasteful path is a two-step cleanup sequence: runtime stabilization first, destructive schema cleanup second.
Key assumptions: you want to minimize repeated strategy loops and keep Replit focused on the real repo only.
Failure points: doing schema deletion before remote/type re-verification, or mixing UI/product rebuild work into the same pass.
Fastest verification: complete Phase 1–3, then re-evaluate remaining schema cleanup from a fresh remote-aligned baseline.